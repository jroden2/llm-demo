spring:
  application:
    name: llm-demo
  data:
    mongodb:
      uri: ${MONGO_URI}
      database: llm-demo

anthropic:
  api-key: ${ANTHROPIC_API_KEY}
  model: claude-sonnet-4-5-20250929
  max-tokens: 1024